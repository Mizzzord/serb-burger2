# 🍔 Serb Burger - Telegram Mini App

Профессиональное веб-приложение для заказа в ресторане "Сербский бургер", оптимизированное для работы внутри Telegram (Mini App).

Клиенты могут быстро заказать бургер, настроить его под себя, оплатить онлайн и получить заказ по QR-коду без очереди.

## 🚀 Основные возможности

- **Интерактивное меню**: Категории товаров (Бургеры, Напитки, Снеки, Соусы) с качественными изображениями.
- **Умный конструктор**: Кастомизация состава бургеров (выбор булочек, котлет, добавок) с мгновенным пересчетом цены.
- **Корзина**: Управление количеством позиций, детальный просмотр состава каждого товара.
- **Онлайн-оплата**: Интеграция с платежной системой **WATA Pay** и поддержка наличных (для заказов до 500₽).
- **Система заказов**: Генерация уникальных QR-кодов для получения заказа в ресторане.
- **Статус-бар**: Плашка с активным заказом отображается на всех страницах до завершения выдачи.
- **Дизайн**: Современный, лаконичный интерфейс на базе шрифта Inter, с приятными анимациями и прелоадером.

## 🛠 Технологический стек

- **Frontend**: Next.js 15 (App Router), React 19, TypeScript
- **Styling**: Tailwind CSS 4, Lucide React (иконки), Framer Motion (анимации)
- **State Management**: Zustand (с сохранением состояния в LocalStorage)
- **Database**: PostgreSQL + Prisma ORM
- **DevOps**: Docker, Docker Compose
- **Payment**: WATA Pay API integration

## 📦 Быстрый старт (Разработка)

1. **Клонируйте репозиторий**:

   ```bash
   git clone https://github.com/your-repo/serb-burger.git
   cd serb-burger
   ```

2. **Установите зависимости**:

   ```bash
   npm install
   ```

3. **Настройте переменные окружения**:

   Создайте файл `.env` и добавьте ключи (см. `.env.example`):

   ```env
   DATABASE_URL="postgresql://user:password@localhost:5432/serb_burger"
   WATA_API_KEY="your_api_key"
   WATA_SHOP_ID="your_shop_id"
   ```

4. **Запустите проект**:

```bash
npm run dev
```

   Откройте [http://localhost:3000](http://localhost:3000).

## 🔐 Безопасность и Админка

- **Панель управления**: Доступна по адресу `/admin`.
- **Защита**: Все админ-роуты защищены Middleware и требуют пароль (настраивается в `.env`).
- **Сессии**: Используются защищенные HTTP-only куки.
- **PWA**: Админку можно установить на Android как отдельное приложение ("SB Admin").

## 🚢 Развертывание (Production)

Приложение полностью готово к профессиональному развертыванию:

- Используется **Standalone** сборка Next.js для максимальной производительности.
- Поддержка **внешних баз данных** PostgreSQL для масштабируемости.
- Подробные инструкции по настройке:
  - [Общая инструкция (VPS/Docker)](./DEPLOYMENT.md)
  - [Инструкция для Timeweb Cloud](./TIMEWEB_DEPLOY.md) 🚀

## 📂 Структура проекта

- `src/app` — Страницы (меню, корзина, успех, админка) и API роуты.
- `src/app/admin` — Панель администратора для управления заказами.
- `src/components` — UI компоненты (карточки, модалки, прелоадер).
- `src/store` — Управление состоянием (корзина, активный заказ).
- `src/lib` — Логика платежей (WATA) и клиент базы данных.
- `src/data` — Данные меню (20+ позиций с картинками).
- `prisma` — Схема базы данных PostgreSQL.

## 📄 Лицензия

Проект разработан для ресторана "Сербский Бургер". Все права на изображения принадлежат их авторам (использованы стоковые фото Unsplash).
